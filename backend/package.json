{
  "name": "backend",
  "version": "1.0.0",
  "description": "Freelance CRM Mini App — Backend API",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "tsx src/index.ts",
    "test": "npm run test:prepare && npm run test:run",
    "test:prepare": "DATABASE_URL=file:./test.db prisma db push",
    "test:run": "DATABASE_URL=file:./test.db NODE_ENV=production AUTH_TOKEN_SECRET=test-auth-token-secret AUTH_TOKEN_TTL_SECONDS=3600 tsx --test src/tests/**/*.test.ts",
    "prisma:migrate": "prisma migrate dev",
    "prisma:generate": "prisma generate",
    "prisma:seed": "tsx prisma/seed.ts",
    "prisma:studio": "prisma studio"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@fastify/cors": "^11.0.1",
    "@prisma/adapter-better-sqlite3": "^7.3.0",
    "@prisma/client": "^7.3.0",
    "dotenv": "^16.5.0",
    "fastify": "^5.3.3"
  },
  "devDependencies": {
    "@types/node": "^25.2.2",
    "prisma": "^7.3.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
